---
id: 'intro'
title: '介绍'
---

# 介绍

让您更容易了解您的数据！

DataVines 是一个易于使用的数据可观测性服务平台，提供全面的数据质量管理能力。作为 DataOps 的核心组件，它确保数据在集成和处理过程中的准确性，帮助用户全面了解和管理数据资产。

## 项目背景

随着大数据技术的发展，数据质量成为企业数字化转型中的关键问题。低质量的数据可能导致决策失误、业务损失和客户信任度下降。传统的数据质量管理工具往往功能单一、扩展性差，难以满足现代企业对数据质量的高要求。

DataVines 应运而生，旨在解决这些问题，提供一个全面、灵活、易用的数据质量服务平台。

## 设计目标

DataVines 的设计目标包括：

- **易用性**：提供直观的 Web 界面，用户可以轻松配置和管理数据质量检查任务
- **灵活性**：支持多种数据源和执行引擎，用户可以根据需求选择合适的配置
- **可扩展性**：采用微内核架构和 SPI 插件机制，方便用户自定义扩展功能
- **高可用性**：支持水平扩展和自动故障转移，确保系统的稳定性和可靠性
- **高性能**：优化数据处理流程，提高数据质量检查的效率

## 核心功能

### 数据目录

DataVines 的数据目录功能定期获取数据源的元数据，构建数据目录，并监控元数据的变化。用户可以通过标签管理功能对元数据进行分类和标注，便于数据的查找和管理。

- **定期获取数据源元数据**，构建数据目录
- **元数据变更监控**，跟踪表结构演变
- **支持标签管理**，便于元数据的分类和标注

### 数据质量

DataVines 内置了 27 种数据质量检查规则，支持 4 种数据质量检查规则类型，包括单表列检查、单表自定义 SQL 检查、跨表准确性检查和两表值比较检查。用户可以设置定时任务进行数据质量检查，并通过 SLA（服务级别协议）对检查结果进行告警。

- **内置 27 种数据质量检查规则**
- **支持 4 种数据质量检查规则类型：**
  - 单表列检查
  - 单表自定义 SQL 检查
  - 跨表准确性检查
  - 两表值比较检查
- **支持定时任务**，定期执行数据质量检查
- **支持 SLA**，对检查结果进行告警

### 数据画像

DataVines 支持定时执行数据探测，生成数据画像报告。平台能够自动识别列类型，匹配相应的数据画像指标，支持表行数趋势监控和数据分布视图。

- **支持定时执行数据探测**，生成全面的报告
- **支持自动识别列类型**，匹配相应的指标
- **支持表行数趋势监控**
- **支持数据分布视图**

### 插件化设计

DataVines 基于插件化设计，多个模块支持用户自定义插件扩展，包括数据源、检查规则、作业执行引擎、告警通道、错误数据存储和注册中心。

- **数据源**：已支持 MySQL、Impala、StarRocks、Doris、Presto、Trino、ClickHouse、PostgreSQL 等
- **检查规则**：内置 27 种检查规则，如空值检查、非空检查、枚举检查等
- **作业执行引擎**：支持 Spark 和 Local 两种执行引擎。Spark 引擎目前仅支持 Spark 2.4 版本，Local 引擎基于 JDBC 开发，无需依赖其他执行引擎
- **告警通道**：支持邮件、钉钉、企业微信、飞书
- **错误数据存储**：支持 MySQL 和本地文件（仅 Local 引擎支持）
- **注册中心**：支持 MySQL、PostgreSQL 和 ZooKeeper

### 多种执行模式

DataVines 提供 Web 页面配置检查作业、运行作业、查看作业执行日志、错误数据和检查结果。同时支持在线生成作业运行脚本，通过 `datavines-submit.sh` 提交作业，可与调度系统结合使用。

- **提供 Web 页面**，配置和管理检查任务
- **支持在线生成作业运行脚本**，通过 `datavines-submit.sh` 提交作业
- **可与调度系统结合使用**

### 容易部署与高可用

DataVines 依赖较少，易于部署。最小化部署仅需依赖 MySQL 即可启动项目，完成数据质量检查操作。支持水平扩展和自动故障转移，确保任务不丢失或重复。

- **依赖较少**，易于部署
- **最小化部署仅需依赖 MySQL**
- **支持水平扩展**，自动故障转移
- **去中心化设计**，Server 节点支持水平扩展以提高性能
- **作业自动故障转移**，确保任务不丢失且不重复执行

## 技术优势

### 微内核架构

DataVines 采用微内核架构模式，将系统分为核心内核和可扩展插件。核心内核负责基础服务、任务调度和生命周期管理，而具体功能如数据源连接、执行引擎、质量指标和通知方式等通过插件形式实现。

主要优势：
- 无需修改核心代码即可添加新的数据源支持
- 插件可以独立开发和测试
- 根据需要选择性部署特定插件

### SPI 插件机制

SPI（Service Provider Interface）机制实现了插件的动态发现和加载。通过 `@SPI` 注解和 `PluginLoader` 类，系统可以：
- 在运行时自动发现和加载插件
- 缓存已加载的插件以提高性能
- 支持单例和新实例创建模式

### 丰富的技术栈

**后端：**
- Spring Boot 提供快速开发和部署能力
- MyBatis-Plus 简化数据库操作
- HikariCP 高性能连接池
- Quartz 任务调度

**前端：**
- React 构建用户界面
- Ant Design 丰富的 UI 组件
- Redux 状态管理
- Webpack 构建优化

## 适用场景

### 企业数据治理

DataVines 适用于企业数据治理，帮助企业在数据集成和处理过程中确保数据的准确性。通过数据目录、数据质量检查和数据画像功能，企业可以全面了解和管理数据资产，提高整体数据质量。

### 数据仓库建设

在数据仓库建设过程中，DataVines 可以作为数据质量检查工具，确保数据仓库中的数据符合预期标准。通过定时任务和 SLA 告警，及时发现和修复数据质量问题。

### 数据湖管理

DataVines 支持多种数据源，适用于数据湖管理。通过插件化设计，可以轻松集成新的数据源，确保数据湖中的数据质量。

### 实时数据处理

DataVines 支持 Spark 和 Flink 等实时计算引擎，适用于实时数据处理场景。通过实时数据质量检查，确保实时数据的准确性和一致性。

## 与其他工具对比

| 特性 | DataVines | 其他工具 A | 其他工具 B |
|---------|-----------|--------------|-------------|
| **易用性** | 直观的 Web 界面，易于配置 | 界面复杂，学习成本高 | 界面简洁但功能有限 |
| **灵活性** | 多种数据源和引擎，插件化设计 | 固定数据源，扩展性差 | 支持部分数据源，扩展性一般 |
| **可扩展性** | 微内核架构，SPI 插件机制 | 无插件机制，难以扩展 | 简单插件机制，扩展性有限 |
| **高可用性** | 水平扩展和自动故障转移 | 单点故障，可靠性低 | 支持集群但故障转移不完善 |
| **性能** | 优化的处理流程 | 处理速度慢，资源消耗大 | 处理速度快但资源消耗高 |

## 快速开始

开始使用 DataVines：

1. **环境要求**：JDK 8、Maven 3.6.1+、MySQL 或 PostgreSQL
2. **构建**：`mvn clean package -Prelease -DskipTests`
3. **部署**：最小化部署仅需 MySQL
4. **配置**：通过 Web 界面设置数据源
5. **运行**：创建和执行质量检查作业

详细的部署说明，请参考我们的[快速入门指南](./03-user-guide/01-quickstart.md)。

## 社区和支持

DataVines 是一个开源项目，欢迎社区贡献。您可以：

- 提交 [Pull Requests](https://github.com/datavane/datavines/pulls) 贡献代码
- 报告 [Issues](https://github.com/datavane/datavines/issues) 提出错误和功能请求
- 加入我们的社区讨论
- 贡献文档改进

有关贡献的更多信息，请查看我们的[开发指南](./06-development/02-environment-preparation.md)。
